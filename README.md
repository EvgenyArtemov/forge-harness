# Forge

A coding agent harness in TypeScript: headless agent core, React Ink CLI, Electron desktop.

Work in progress — see [docs/roadmap.md](docs/roadmap.md).

## Setup
```bash
pnpm install
cp .env.example .env   # add your DEEPSEEK_API_KEY
pnpm hello
```
